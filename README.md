1. run python file in port 8000  ( download requirements fast api, uvicorn, youlo08 etc)
   use termical command:  uvicorn main:app --reload --host 0.0.0.0 --port 8000
   open in browser: http://localhost:8000/docs

2. run ProctorControllerApplication.java
   open in browser: http://localhost:8080

all tab changing logics are in index.html script tag. just change the ui as your wish.
